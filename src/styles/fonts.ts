import { Roboto, Lato } from 'next/font/google';

export const roboto = Roboto({
  subsets: ['latin'],
  weight: '300',
  display: 'swap',
});

export const lato = Lato({
  subsets: ['latin'],
  weight: '400',
  display: 'swap',
});